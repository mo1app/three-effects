import type { Object3D } from "three";

/**
 * Placeholder hook — wire your effects to a scene root here.
 */
export function attachExample(_root: Object3D): void {
  // no-op until implementations land
}
